# AWK
## look and feel
```awk
awk '{ if (length($0) > max) max = length($0) } END { print max }' geeksforgeeks.txt
```
## Source
```
awk '{ if (length($0) > max) max = length($0) } END { print max }' geeksforgeeks.txt
```
